# Dependencies (installed in image)
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# CRA output (built inside Docker)
build

# Tests & tooling
coverage
cypress
**/*.test.*
**/*.spec.*

# Environment (never copy secrets into the image)
.env
.env.*
!.env.example

# Docker / CI
Dockerfile*
docker-compose*.yml
.dockerignore

# IDE & OS
.vscode
.idea
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Git
.git
.gitignore
